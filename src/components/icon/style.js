import styled from 'styled-components/macro';

const StyledIcon = styled.div`
  width: var(--icon-size);
  height: var(--icon-size);
  color: currentColor;

  path {
    fill: currentColor;
  }
`;

export {
  StyledIcon,
};
